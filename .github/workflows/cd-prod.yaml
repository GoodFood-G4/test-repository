name: Deploy PROD

on:
  push:
    branches:
      - 'release.*'
  workflow_dispatch:

jobs:

  ci:
    uses: ./.github/workflows/ci.yml

  deploy:
    name: Deploy to PROD
    runs-on: ubuntu-latest
    needs: ci
    environment: prod

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: build-artifact

      - name: Simulate PROD deployment
        run: |
          echo "======================"
          echo "DEPLOYMENT - PROD"
          echo "Deploying release to production servers"
          echo "Restarting production services"
          echo "======================"